// TODO item
export const getItem = (label, key, icon, children, type) => {
  return { label, key, icon, children, type };
};

// TODO items
export const items = [
  // getItem("PE", "1"),
  // getItem("English", "2"),
  // getItem("Mathematics", "3"),
  // getItem("Science", "4"),
  // getItem("Araling Panlipunan", "5"),
];
